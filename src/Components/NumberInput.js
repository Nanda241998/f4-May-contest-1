import React from 'react';

const NumberInput = ({ label, value, onChange }) => {
  return (
    <div>
    
      <input type="Num 1" value={value} onChange={onChange} />
    </div>
  );
};

export default NumberInput;
